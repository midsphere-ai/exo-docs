spec:
  name: exo-docs
  static_sites:
    - name: exo-docs
      git:
        branch: main
        repo_clone_url: https://github.com/Midsphere-AI/exo-docs.git
      build_command: npm install && npm run build
      output_dir: dist
      environment_slug: node-js
      routes:
        - path: /
